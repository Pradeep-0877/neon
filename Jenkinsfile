pipeline{
    agent any
    environment{ //now we are defining environment in pipeline scope
       name='pradeep'

       course='GCP'

    }
    stages{
        stage('Build'){
            steps{
                echo "eslcome ${name}"
                echo "you enrolled in ${course} course"

            }
        }
    }
}
