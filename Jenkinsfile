pipeline{
    agent any
    stages{
        stage('A'){
            steps{
                echo "executing multi branch pipeline"
                echo 'its working fine'
            }
        }
        stage('B'){
            steps{
                echo "executing multi branch pipeline"
                echo 'it is a on different branch'
            }
        }
    }
}
