pipeline{
    agent any
    stages{
        stage('A'){
            steps{
                echo "executing multi branch pipeline"
                echo 'its working fine
            }
        }
    }
}
